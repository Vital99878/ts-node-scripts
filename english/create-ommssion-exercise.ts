#!/usr/bin/env ts-node
import * as fs from 'fs';
import * as path from 'path';

function createOmissionExercise() {
  const currentDirectory = process.cwd();
  const [exercise, executedExercise] = process.argv.slice(2);
}

createOmissionExercise();
